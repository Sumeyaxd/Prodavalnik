spring:
  datasource:
    url: jdbc:hsqldb:mem:prodavalnik;DB_CLOSE_DELAY=-1;sql.syntax_mys=true
    driver-class-name: org.hsqldb.jdbcDriver
    username: test
    password: test
  jpa:
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: never

server:
  port: 8080

mail:
  prodavalnik: "test@prodavalnik.com"
  host: localhost
  port: 1212
  username: test@prodavalnik.com
  password: 1234
